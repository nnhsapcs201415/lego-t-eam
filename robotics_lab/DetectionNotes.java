
public class DetectionNotes
{
    /* To help us remember how to use detectors */
    public static void main(String[] args) {
        /*
         * UltrasonicSensor ultraSensor = new UltrasonicSensor(SensorPort.S4);
         * FeatureDetector fd = new RangeFeatureDetector(ultraSensor, max distance, period (in millisecs));
         * 
         * Feature result = fd.scan();
         * make sure to check for a null result before doing...
         * result.getRangeReading().getRange();
         * 
         * 
         * 
         */
    }
    
    // also, remember to get the wheel diameter and track width to modify programs from day 2
}
